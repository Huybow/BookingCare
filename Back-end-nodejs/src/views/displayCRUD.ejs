<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>GET CRUD</title>
</head>
<style>
    table,
    th,
    td {
        border: 1px solid black;
        text-align: center;
    }
    .btn-edit {
        outline: none;
        border: none;
        background: rgb(18, 56, 227);
        height: 25px;
        width: 60px;
        border-radius: 3px;
        cursor: pointer;
        color: white;
    }
    .btn-delete {
        outline: none;
        border: none;
        background: red;
        height: 25px;
        width: 60px;
        border-radius: 3px;
        cursor: pointer;
        color: white;
    }
</style>

<body>
    <h1>Get crud from view</h1>
    <table style="width:100%">
        <tr>
            <th>Email</th>
            <th>First Name</th>
            <th>Last Name</th>
            <th>Address</th>
            <th>Actions</th>
        </tr>

        <% for(let i=0; i < dataTable.length; i++) { %>
            <tr>
                <td>
                    <%= dataTable[i].email %>
                </td>
                <td>
                    <%= dataTable[i].firstName %>
                </td>
                <td>
                    <%= dataTable[i].lastName %>
                </td>
                <td>
                    <%= dataTable[i].address %>
                </td>
                <td>
                    <a href="/edit-crud?id=<%= dataTable[i].id  %>" class="btn-edit">Edit</a>
                    <a href="/delete-crud?id=<%= dataTable[i].id  %>" class="btn-delete">Delelte</a>
                </td>
            </tr>
            <% } %>

    </table>
</body>

</html>